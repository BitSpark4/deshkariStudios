// Phase 5 fills the Portfolio page (filter + grid + CTA).
export default function Portfolio() {
  return (
    <div className="container-x py-section">
      <p className="section-label">Portfolios</p>
      <h1 className="text-h2 text-heading">Our Work</h1>
      <p className="mt-4 text-body">[Phase 5 — Portfolio filter + grid go here.]</p>
    </div>
  );
}
